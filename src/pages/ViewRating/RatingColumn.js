import React from 'react';
import {Grid, Button, Container, Typography, Box} from "@material-ui/core";

export default function RatingColumn(props){
  let ratingFields = props.ratingFields;
    return (
        <>
        <Grid></Grid>
        </>
    );
}